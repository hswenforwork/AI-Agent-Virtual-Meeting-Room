# 前端可公開（這些值本來就會被瀏覽器看到，安全性靠 Supabase RLS，不是靠藏住它們）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_APP_BASE_PATH=/

# 以下只在 Supabase Edge Function 的 secrets 設定，絕對不要放進這個檔案的實際值、
# 也絕對不要 commit 到 GitHub。用 `supabase secrets set` 或 Supabase Dashboard 設定。
# 注意：SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY 是 Supabase 保留字，
# 平台會自動注入給 Edge Function，不能也不需要手動設定。
#
# BYOK 上線後（brainstorms/2026-09-22-user-api-key-settings.md），Anthropic／OpenAI／Google
# 的 API key 不再是部署者要設定的 Edge Function secret，是每個使用者自己登入後到網站的
# 「設定」頁輸入，加密存進 Supabase Vault（見 supabase/migrations/0009_byok_api_keys.sql）。
# ALLOWED_ORIGINS=
# DEFAULT_CLAUDE_MODEL=claude-sonnet-5
# DEFAULT_GPT_MODEL=gpt-5.1
# DEFAULT_GEMINI_MODEL=gemini-3.8-flash
# DEFAULT_WORKER_AGENT_MODEL=claude-opus-5
# MAX_AGENT_RUNS_PER_MESSAGE=4
#
# 以上都只是使用者還沒在「設定」頁選過模型時的兜底預設值（見
# brainstorms/2026-09-22-provider-model-selection.md）；使用者選過之後就會用他自己選的。
#
# 以下兩個是選用的：讓工作型代理能直接修改這個專案自己的 GitHub repo（見 README「附加設定：工作型代理」）。
# GITHUB_REPO_URL=
# GITHUB_TOKEN=
# GITHUB_REPO_BRANCH=
